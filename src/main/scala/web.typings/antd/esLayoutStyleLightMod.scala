package web.typings.antd

import web.typings.antd.esLayoutStyleMod.LayoutToken
import web.typings.antd.esThemeInterfaceMod.GenerateStyle
import org.scalablytyped.runtime.Shortcut
import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.`|`
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

object esLayoutStyleLightMod extends Shortcut {
  
  @JSImport("antd/es/layout/style/light", JSImport.Default)
  @js.native
  val default: GenerateStyle[
    LayoutToken, 
    /* import warning: transforms.QualifyReferences#resolveTypeRef many Couldn't qualify CSSObject */ Any
  ] = js.native
  
  type _To = GenerateStyle[
    LayoutToken, 
    /* import warning: transforms.QualifyReferences#resolveTypeRef many Couldn't qualify CSSObject */ Any
  ]
  
  /* This means you don't have to write `default`, but can instead just say `esLayoutStyleLightMod.foo` */
  override def _to: GenerateStyle[
    LayoutToken, 
    /* import warning: transforms.QualifyReferences#resolveTypeRef many Couldn't qualify CSSObject */ Any
  ] = default
}
