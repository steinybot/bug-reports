package web.typings.antd

import web.typings.antd.esNotificationStyleMod.NotificationToken
import web.typings.antd.esThemeInterfaceMod.GenerateStyle
import org.scalablytyped.runtime.Shortcut
import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.`|`
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

object esNotificationStylePlacementMod extends Shortcut {
  
  @JSImport("antd/es/notification/style/placement", JSImport.Default)
  @js.native
  val default: GenerateStyle[
    NotificationToken, 
    /* import warning: transforms.QualifyReferences#resolveTypeRef many Couldn't qualify CSSObject */ Any
  ] = js.native
  
  type _To = GenerateStyle[
    NotificationToken, 
    /* import warning: transforms.QualifyReferences#resolveTypeRef many Couldn't qualify CSSObject */ Any
  ]
  
  /* This means you don't have to write `default`, but can instead just say `esNotificationStylePlacementMod.foo` */
  override def _to: GenerateStyle[
    NotificationToken, 
    /* import warning: transforms.QualifyReferences#resolveTypeRef many Couldn't qualify CSSObject */ Any
  ] = default
}
