package web.typings.antd

import web.typings.antd.esTableStyleMod.TableToken
import web.typings.antd.esThemeInterfaceMod.GenerateStyle
import org.scalablytyped.runtime.Shortcut
import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.`|`
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

object esTableStyleSelectionMod extends Shortcut {
  
  @JSImport("antd/es/table/style/selection", JSImport.Default)
  @js.native
  val default: GenerateStyle[
    TableToken, 
    /* import warning: transforms.QualifyReferences#resolveTypeRef many Couldn't qualify CSSObject */ Any
  ] = js.native
  
  type _To = GenerateStyle[
    TableToken, 
    /* import warning: transforms.QualifyReferences#resolveTypeRef many Couldn't qualify CSSObject */ Any
  ]
  
  /* This means you don't have to write `default`, but can instead just say `esTableStyleSelectionMod.foo` */
  override def _to: GenerateStyle[
    TableToken, 
    /* import warning: transforms.QualifyReferences#resolveTypeRef many Couldn't qualify CSSObject */ Any
  ] = default
}
